import React, { Component } from "react"; 

import NaviagationBar from "./NavigationBar/NavigationBar";

export default class HomePage extends Component { 
    render() {
        return (
            <NaviagationBar />
        );
    }
}